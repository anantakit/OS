server:
  port: 8080
  servlet:
    contextPath: /api

spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://mysql:3306/test
    username: root
    password: keep1234
    driver-class-name: com.mysql.jdbc.Driver

  # security:
  #   user:
  #     name: frame
  #     password: frame